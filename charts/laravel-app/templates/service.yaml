apiVersion: v1
kind: Service
metadata:
  name: {{ include "laravel-app.name" . }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ include "laravel-app.name" . }}
  ports:
  - port: 80
    targetPort: {{ .Values.containerPort }}
